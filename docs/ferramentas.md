# Ferramentas

SINTEGRA: PR expõe 1 ferramenta (todas somente leitura).

### 1. `sintegra_pr_consultar`
**Input**: `cnpj` (opcional), `ie` (opcional), `ie_produtor` (opcional), `cnpj_produtor` (opcional), `cpf` (opcional)

SINTEGRA: PR, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SINTEGRA: PR
```
